args@{
  config,
  pkgs,
  lib,
  inputs ? null,
  isLinux,
  mkApp,
  ...
}:

mkApp {
  _file = toString ./.;
  name = "PackageName";
  packages = pkgs: [ pkgs.PackageToReplace ];
  description = "PackageDescription";
} args
